@model GameSource.ViewModels.GameSource.GenreViewModel.GenreEditViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1 class="text-center">Edit Genre</h1>
<hr />

<div class="container">
    <div class="row">
        <div class="col-md-4">
            <form asp-action="Edit" class="dialog-form">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="Genre.ID" />
                <div class="form-group">
                    <label asp-for="Genre.Name" class="control-label dialog-label"></label>
                    <input asp-for="Genre.Name" class="form-control dialog-input dialog-input-base"/>
                    <span asp-validation-for="Genre.Name" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Save changes" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>

    <div class="row">
        <a asp-action="Index">Back to Index</a>
    </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}