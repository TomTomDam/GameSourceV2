@model GameSource.ViewModels.GameSource.PlatformTypeViewModel.PlatformTypeCreateViewModel

@{
    ViewData["Title"] = "Add a new Platform Type";
}

<h1 class="text-center">Add a new Platform Type</h1>

<div class="container">
    <div class="row">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="PlatformType.ID" />
            <div class="form-group">
                <label asp-for="PlatformType.Name" class="control-label"></label>
                <input asp-for="PlatformType.Name" class="form-control" />
                <span asp-validation-for="PlatformType.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Submit" class="btn btn-primary" />
            </div>
        </form>
    </div>

    <div>
        <a asp-action="Index">Back to Index</a>
    </div>
</div>