@model GameSource.ViewModels.GameSource.PublisherViewModel.PublisherEditViewModel

@{
    ViewData["Title"] = "Edit " + Model.Publisher.Name;
}

<div class="container edit-page page-content">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="page-heading">Edit Publisher</div>
            <hr />
            <form asp-action="Edit" class="dialog-form">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="Publisher.ID" />
                <div class="form-group">
                    <label asp-for="Publisher.Name" class="control-label dialog-label"></label>
                    <input asp-for="Publisher.Name" class="form-control dialog-input dialog-input-base" />
                    <span asp-validation-for="Publisher.Name" class="text-danger"></span>
                </div>
                <div class="submit-btn-section">
                    <div class="form-group">
                        <input type="submit" value="Save changes" class="btn btn-primary" />
                        <a class="btn btn-primary index-link" asp-action="Index">Back to Index</a>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}