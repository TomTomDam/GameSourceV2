@model GameSource.ViewModels.GameSource.PublisherViewModel.PublisherDeleteViewModel

@{
    ViewData["Title"] = "Delete";
}

<h1 class="text-center">Delete</h1>
<hr />

<div class="container">
    <div class="row">
        <form asp-action="Delete">
            <p>Are you sure you want to delete this Publisher?</p>
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Publisher.ID" />
            <div class="form-group">
                <label asp-for="Publisher.Name" class="control-label font-weight-bold"></label>
                <p>@Model.Publisher.Name</p>
            </div>
            <div class="form-group">
                <input type="submit" value="Delete" class="btn btn-primary" />
            </div>
        </form>
    </div>

    <div class="row">
        <a asp-action="Index">Back to Index</a>
    </div>
</div>